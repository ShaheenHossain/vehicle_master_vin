<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Create a group for vehicle management -->
    <record id="group_vehicle_manager" model="res.groups">
        <field name="name">Vehicle Manager</field>
        <field name="category_id" ref="base.module_category_extra"/>
        <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
    </record>

    <record id="group_vehicle_user" model="res.groups">
        <field name="name">Vehicle User</field>
        <field name="category_id" ref="base.module_category_extra"/>
        <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
    </record>

    <!-- Record Rules -->
    <record id="vehicle_master_rule" model="ir.rule">
        <field name="name">Vehicle: See own vehicles</field>
        <field name="model_id" ref="model_vehicle_master"/>
        <field name="domain_force">[('create_uid', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('group_vehicle_user'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

    <!-- Allow managers to see all vehicles -->
    <record id="vehicle_master_manager_rule" model="ir.rule">
        <field name="name">Vehicle: See all vehicles</field>
        <field name="model_id" ref="model_vehicle_master"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('group_vehicle_manager'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>
</odoo>